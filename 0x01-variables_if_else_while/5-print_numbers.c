#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - prints the sign of the number
 *
 * Return: 0
*/
int main(void)
{
	char nbr = 0;

	while (nbr < 10)
	{
		printf("%d", nbr);
		nbr++;
	}
	putchar('\n');
	return (0);
}
